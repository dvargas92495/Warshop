using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JaguarController : RobotController
{

    // Use this for initialization
    void Start()
    {
        displayName = "Jaguar";
        displayAbbreviation = "JAG";
        description = "Could Move a third time instead of Attack";
    }

    // Update is called once per frame
    void Update()
    {

    }
}
